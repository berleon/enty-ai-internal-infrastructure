apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This kustomization uses KSOPS to decrypt secrets during kustomize build
# KSOPS acts as a generator plugin, not a resource transformer

generators:
  - ./secret-generator.yaml
