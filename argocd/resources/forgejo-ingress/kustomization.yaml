apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tailscale ingress for Forgejo
# Exposes the Forgejo Git server via Tailscale mesh network
# Note: Ingress requires HTTPS enabled on tailnet for TLS certificates

resources:
  - forgejo-tailscale-ingress.yaml
