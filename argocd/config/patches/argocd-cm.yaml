apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
data:
  # Enable kustomize plugins and exec for KSOPS
  kustomize.buildOptions: "--enable-alpha-plugins --enable-exec"
  kustomize.version: "v5.0.0"

  # Configure SOPS decryption
  # KSOPS will automatically decrypt files matching *.yaml that contain sops metadata
